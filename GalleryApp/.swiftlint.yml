# .swiftlint.yml

excluded:
  - Pods
  - .build
  - DerivedData
  - build

disabled_rules:
  - trailing_whitespace
  - line_length
  - function_body_length
  - file_length

opt_in_rules:
  - empty_count
  - closure_spacing
  - explicit_init
  - fatal_error_message

reporter: "xcode"
